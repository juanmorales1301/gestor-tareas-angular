import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'gestor-de-tareas',
  webDir: 'dist/gestor-de-tareas/browser',
  bundledWebRuntime: false
};

export default config;
